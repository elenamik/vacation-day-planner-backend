import React, { Component } from 'react';

class Form extends Component{
    //called by day
    // handles input
    // will send update back to day (return val)
    render(){
        
    }
}

export default Form;